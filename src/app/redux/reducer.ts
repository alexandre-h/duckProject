// todo-app-angular/src/app/redux/reducer.ts
export const todos = (state = [], action) => {
  switch (action.type) {
    case 'ADD_INFO':
      // define action
      break;
    default:
      return state;
  }
}
